#!/usr/bin/env bash
set -euo pipefail

# The PDF preview is supervised through .amp/services.yaml, not this hook.
# Keep resume fast; no service restart or dependency installation is needed.
command -v typst >/dev/null
command -v python3 >/dev/null
